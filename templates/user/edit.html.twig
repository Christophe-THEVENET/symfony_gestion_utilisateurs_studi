




{# utilisateur ne peux pas modifier celui d un autre
    
    {% if app.user.username is same as(post.user.username) %} dans le index.html.twig des posts #}










{% extends 'base.html.twig' %}

{% block title %}Modifier{% endblock %}

{% block stylesheets %}
{{ parent() }}
<link rel="stylesheet" href="{{ asset('css/register.css') }}">
{% endblock %}


{% block body %}

<section>
   
    <div class="example-wrapper mb-5">
        <h1>Modifier✅</h1>
    </div>


    <div class="alert-password" id="alert">
        Les mots de passe ne sont pas identiques
    </div>



    {{ form_start(registrationForm, {'attr': {'id': 'register-form'}}) }}
    {# utilisation de classes bootstrap pour la mise en forme #}
    <div>
        {{ form_errors(registrationForm.username, {'attr': {'id': 'alert-username'}}) }}
        {{ form_label(registrationForm.username) }}
        {{ form_widget(registrationForm.username) }}
    </div>


    <div>
        {{ form_errors(registrationForm.Roles, {'attr': {'id': 'alert-username'}}) }}
        {{ form_label(registrationForm.Roles) }}
        {{ form_widget(registrationForm.Roles) }}
    </div>
   {#  <div>
        {{ form_errors(registrationForm.plainPassword, {'attr': {'id': 'alert-password'}}) }}
        {{ form_label(registrationForm.plainPassword) }}
        <div class="mdp1">
            {{ form_widget(registrationForm.plainPassword) }}
            <i class="fa-solid fa-eye" id="eye1"></i>
        </div>
    </div>
    <div>
        <label for="verifpass">Saisir une seconde fois le mot de passe</label>
        <div class="mdp2">
            <input type="password" id="verifpass" required class="form-control">
            <i class="fa-solid fa-eye" id="eye2"></i>
        </div>
    </div>
    <div>
        {{ form_label(registrationForm.agreeTerms) }}
        {{ form_widget(registrationForm.agreeTerms, {'attr': {'class': 'my-3'}}) }}
    </div> #}

    <button type="submit" class="btn btn-success m-2" id="register-button">{{ button_label|default('Modifier l\'utilisteur')
        }}</button>
    {{ form_end(registrationForm) }}

    <a href="{{ path('app_home') }}">Revenir à l'acceuil</a>







</section>

<script src="{{ asset('script/register.js') }}"></script>
{% endblock %}